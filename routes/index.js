const authRoute = require('./authRoute')
const questionRoute = require('./questionRoute')
const userRoute = require('./userRoute')
const submitRoute = require('./submitRoute')
const defaultRoute = require('./defaultRoute')
module.exports = {
    authRoute,
    questionRoute,
    userRoute,
    submitRoute,
    defaultRoute,
}
